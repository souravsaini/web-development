<%- include('partials/header') -%>

<h1>Home</h1>
<p> <%= homeStartingContent %> </p>
<% posts.forEach(function(post) { %>
  <h1> <%= post.postTitle %> </h1>

  <p> <%= post.postBody.substring(0, 100) + " ..."%> <!-- truncate a string to 100 characters -->
    <a href="/posts/<%= post.postTitle %>"> Read More</a> <!-- redirect to /posts/<postTitle> -->
  </p>
<% }) %>

<%- include('partials/footer') -%>
